services:
  floci:
    image: floci/floci:latest
    ports:
      - "4566:4566"
    environment:
      - LOCALSTACK_HOST=floci
      - FLOCI_HOST=floci
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock